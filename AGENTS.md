# Agent Operating Context

This repository is operated from `C:\Users\PC\Desktop\GitGov` on Windows PowerShell.

## Resume First

- Read `docs/CURRENT_CONTEXT.md` first when resuming a paused or transferred session. It is the compact handoff for exact current state, latest commits/PRs, non-negotiable operating decisions, access summary, validation commands, and next steps.
- Keep `docs/CURRENT_CONTEXT.md` current after major access/configuration/deployment/validation changes.
- Use `docs/AGENT_PUBLIC_CONTEXT.md` as the safe public context bridge for external agents. Do not force-add restricted forensic/strategy docs just because another agent cannot see ignored local files.

## Access

- GitHub CLI is installed at `C:\Users\PC\Tools\gh\bin\gh.exe`.
- `gh` is authenticated as `yohandry10` with admin access to `yohandry10/Git-Gov`.
- GitHub token scopes observed: `repo`, `workflow`, `read:org`, `gist`, `admin:repo_hook`.
- Render API access is available via local ignored env files only. Do not commit or print token values.
- Local Render env key name: `RENDER_API_KEY`.
- Local GitGov API env key name: `GITGOV_API_KEY`.
- Local SonarQube API access is available when `SONAR_HOST_URL`, `SONAR_TOKEN`, and `SONAR_PROJECT_KEY` are loaded from ignored local env files.
- Jenkins direct API access is available when `JENKINS_SERVER_URL`, `JENKINS_USER`, and `JENKINS_API_TOKEN` are loaded from ignored local env files.
- Jira Cloud API access is available when `JIRA_BASE_URL`, `JIRA_EMAIL`, `JIRA_API_TOKEN`, and `JIRA_PROJECT_KEY` are loaded from ignored local env files.
- Local ignored env files currently used by the agent:
  - `C:\Users\PC\Desktop\GitGov\gitgov\.env`
  - `C:\Users\PC\Desktop\GitGov\gitgov\gitgov-server\.env`
- Treat these local env files as the source of truth for provider tokens. Never print token values from them.

## Agent Capabilities

- GitHub:
  - Inspect repo state, branches, commits, PRs, checks, branch protection, and workflow runs through `gh`.
  - Create branches, push commits, open PRs, merge PRs when checks pass and user intent is clear.
  - Read GitHub Actions logs and rerun workflows when needed.
  - Manage repository Actions variables and secrets when explicitly requested; secret creation or updates are sensitive operations.
  - Manage repository webhooks through `gh` when explicitly requested; webhook secrets must come from ignored local env files only.
- Render:
  - Query service metadata, deployments, logs, and health through the Render API.
  - Verify the deployed backend at `https://gitgov-api.onrender.com`.
  - Trigger deploys or inspect deploy failures when `RENDER_API_KEY` is present locally.
- Local SonarQube:
  - Access local SonarQube at `http://localhost:9000`.
  - Authenticate by API using `SONAR_TOKEN` from ignored env files.
  - Query project quality gate, measures, issues, hotspots, and analysis status for `SONAR_PROJECT_KEY=yohandry10_git-gov`.
  - Use the browser session for UI-only operations when `@browser-use` is explicitly available.
- Jenkins:
  - Access local Jenkins at `http://localhost:8096`.
  - Authenticate by API as `admin` using `JENKINS_API_TOKEN` from ignored env files.
  - Inspect job metadata, build history, build logs, queue state, and build results.
  - Current job name: `gitgov-demo-pipeline`.
  - Trigger Jenkins builds by authenticated API when requested. Trigger-only `/build?token=...` needs `JENKINS_BUILD_TRIGGER_TOKEN` if that flow is required.
- Jira:
  - Access Jira Cloud at `https://yohandrychirinos1.atlassian.net`.
  - Authenticate by API using `JIRA_EMAIL` and `JIRA_API_TOKEN` from ignored env files.
  - Query project metadata, issue types, issues, and comments for `JIRA_PROJECT_KEY=KAN`.
  - Create Jira issues and comments by API when explicitly requested.
  - Native Jira webhook target is `https://gitgov-api.onrender.com/webhooks/jira?org_name=yohandry10` once deployed.
  - Native Jira webhook authentication uses `X-Hub-Signature` HMAC with `JIRA_WEBHOOK_SECRET`; do not use `GITGOV_API_KEY` in the native Jira webhook URL.
  - Use Jira ticket IDs in branch names, commit messages, PR titles, and PR comments to generate GitGov traceability evidence.
- Local stack:
  - Use Docker Compose profiles `jenkins` and `sonar` to start Jenkins and SonarQube.
  - Validate local services before editing CI/CD configuration.
  - On 2026-04-28 Docker Desktop was started successfully and `docker compose --profile sonar --profile jenkins up -d sonarqube-db sonarqube jenkins` brought the local validation stack online.

## GitHub Repository

- Repository: `yohandry10/Git-Gov`
- Default branch: `main`
- Branch protection is enabled for `main`.
- Required status checks currently include:
  - `Security Guard`
  - `Server Clippy + Check`
  - `Desktop Rust Clippy`
  - `Frontend Lint + Typecheck`
  - `Website Lint + Typecheck + Build`
  - `Validate quality_gates warn/block matrix`
- Admin enforcement is enabled.
- Required status checks are strict.
- `Security Guard` also enforces traceability hygiene:
  - Branch names must include a Jira-style ticket ID such as `KAN-4` except protected/base branches like `main`.
  - Pull request titles must include a Jira-style ticket ID such as `KAN-4`.
  - New commit messages in PRs/pushes must include a Jira-style ticket ID.
  - Local equivalent: `scripts/github/check_traceability_policy.ps1`.
  - Local preflight equivalent: `scripts/security/publication_guard.ps1`.
  - Local commit hook: `.githooks/commit-msg` when `core.hooksPath=.githooks`.

## GitHub Webhooks

- Primary GitHub webhook ID: `610772988`.
- Primary GitHub webhook URL: `https://gitgov-api.onrender.com/webhooks/github`.
- Render has `GITHUB_WEBHOOK_SECRET` configured for `gitgov-api`; do not print or commit the value.
- Configured events: `push`, `create`, `pull_request`, `pull_request_review`, `pull_request_review_comment`, `issue_comment`, `check_run`, `check_suite`, and `status`.
- GitHub webhook delivery has been validated with real repository events returning HTTP `200`.
- PR merge materialization is idempotent: duplicate `pull_request` deliveries for merged PRs should still repair `pull_request_merges` and ticket correlations.
- Production PR-title validation completed with Jira ticket `KAN-4`:
  - GitHub merged PR titles containing `KAN-4` were ingested through real webhook deliveries.
  - Webhook redelivery for a merged PR returned `processed=true`.
  - `pull_request_merges` reached at least `2` records in production validation.
  - Jira backfill scanned `2` merged PRs and created `2` PR-title correlations.
  - `commit_ticket_correlations.source` must remain `pr_title`; production DB constraints allow `branch_name`, `commit_message`, `pr_title`, and `manual`.
- Ticket coverage semantics include PR merge evidence:
  - `GET /integrations/jira/ticket-coverage` builds its commit universe from both `client_events(event_type='commit')` and `pull_request_merges`.
  - For PR merge evidence, coverage uses `merge_commit_sha` from payload first and falls back to `head_sha`.
  - This lets PR-title correlations count toward Jira coverage even when the merge commit was only observed through GitHub webhook evidence.

## Render

- Primary backend service: `gitgov-api`
- Primary backend URL: `https://gitgov-api.onrender.com`
- Render service ID: `srv-d7lgtc77f7vs73b38uqg`
- Render service type: Docker web service.
- Render region: Oregon.
- Render deploy branch: `main`.
- Render root directory: `gitgov/gitgov-server`.
- Render service is reachable through the Render API using `RENDER_API_KEY` from ignored local env files.
- `docs/DEPLOYMENT.md` treats Render as the current production route; the former EC2/Nginx/systemd material is retained as legacy/self-hosted guidance only.

## Jira

- Jira base URL: `https://yohandrychirinos1.atlassian.net`
- Jira project name: `GitGov`
- Jira project key: `KAN`
- Jira project ID: `10000`
- Available issue types observed by API: `Epic`, `Subtask`, `Tarea`, `Historia`.
- Jira credentials are stored only in ignored local env files:
  - `JIRA_BASE_URL`
  - `JIRA_EMAIL`
  - `JIRA_API_TOKEN`
  - `JIRA_PROJECT_KEY`
- GitGov native Jira webhook endpoint after deployment: `https://gitgov-api.onrender.com/webhooks/jira?org_name=yohandry10`.
- GitGov admin/manual Jira ingest endpoint remains `https://gitgov-api.onrender.com/integrations/jira` and requires Bearer admin auth plus `x-gitgov-jira-secret` when `JIRA_WEBHOOK_SECRET` is configured. Global admin keys also require an `org_name` payload hint such as `yohandry10`.
- Native Jira webhook setup should use Jira's webhook secret field with the same value as Render `JIRA_WEBHOOK_SECRET`.
- Native Jira webhook is configured in Jira Cloud:
  - Webhook name: `GitGov signed issue sync`
  - Webhook ID: `1`
  - Events: `jira:issue_created`, `jira:issue_updated`, `jira:issue_deleted`
  - JQL filter: `project = KAN`
  - Status: enabled and signed.
- Render has `JIRA_WEBHOOK_SECRET` configured for `gitgov-api`; do not print or commit the value.
- Domain SLO targets in `ops/slo/domain-slo-targets.json` must include `org_name=yohandry10`; leaving it blank causes SLO validation to read unscoped telemetry and overstate traceability gap.
- Run `scripts/control-plane/validate_domain_slo_target_config.ps1 -RequireOrgName -RequireRepoFullName -RequireBranch` after editing SLO targets; CI enforces the same static guardrail before live validation.
- Traceability validation tickets created by API:
  - `KAN-4` - Validate GitGov traceability through PR titles
  - `KAN-5` - Validate GitGov traceability through PR comments
  - `KAN-6` - Validate GitGov release readiness evidence

## GitHub Actions Configuration

- Repository secret required by GitGov workflows: `GITGOV_API_KEY`.
- Repository variable required by GitGov workflows: `GITGOV_URL=https://gitgov-api.onrender.com`.
- SonarCloud is not used for this repository because the current GitHub repository/account is personal, not organizational. Treat this as a closed decision for this repo: do not ask again to use SonarCloud and do not propose SonarCloud onboarding unless the repository is moved to a GitHub organization.
- Local SonarQube is the selected Sonar runtime. Repository variable `SONAR_HOST_URL=http://localhost:9000`; GitHub-hosted Sonar scan must skip unless a self-hosted runner can reach that host.
- Sonar variable for local/runtime use: `SONAR_PROJECT_KEY=yohandry10_git-gov`.
- GitHub Actions `SONAR_TOKEN` is optional while SonarQube remains local; do not force it for GitHub-hosted runners because the scan is expected to skip when `SONAR_HOST_URL` is localhost.
- The quality gate policy matrix workflow is optional at workflow level but its job is required by branch protection.
- The matrix workflow must run on both `pull_request` and `push` to `main`; otherwise PR merges can be blocked by a required check that never appears.
- Release Readiness Gate is advisory by default on `push`; use manual `workflow_dispatch` with `enforce_gate=true` when a release must be blocked by readiness score.
- Release Readiness Gate also runs daily by schedule at `10:17 UTC`; scheduled runs refresh Jira/PR correlations before scoring and enforce the standard readiness target.
- Push/manual Release Readiness Gate runs remain advisory unless `enforce_gate=true`; a failed pre-score Jira correlation refresh is only blocking when the gate is enforced.
- Manual Release Readiness Gate runs default to a 720h lookback window and can disable the pre-score Jira correlation refresh with `refresh_jira_correlations=false` if an operator needs a pure read-only check.
- First-party GitHub Actions were upgraded for Node 24 action-runtime compatibility:
  - `actions/checkout@v6`
  - `actions/setup-node@v6`
  - `actions/upload-artifact@v7`
  - `pnpm/action-setup@v5`
  - `node-version: 20` remains the project build runtime where configured; it is not the internal runtime of `actions/*`.

## External Service Credentials

- Local SonarQube API access is configured through ignored env files. Current local values use `SONAR_HOST_URL=http://localhost:9000` and `SONAR_PROJECT_KEY=yohandry10_git-gov`.
- Current local SonarQube token name: `gitgov-local`.
- Current local SonarQube token expires on May 22, 2026.
- Jenkins read/build access is configured through ignored env files with `JENKINS_SERVER_URL=http://localhost:8096`, `JENKINS_USER=admin`, `JENKINS_API_TOKEN`, and `JENKINS_JOB_NAME=gitgov-demo-pipeline`.
- Current Jenkins API token name: `codex-local`.
- Jenkins authenticated API access through `JENKINS_API_TOKEN` is already configured and is the normal agent path for inspection, logs, queue state, build history, and authenticated build operations.
- Jenkins trigger-only access can use `JENKINS_JOB_NAME` and `JENKINS_BUILD_TRIGGER_TOKEN`, but that only starts builds through `/job/{job}/build?token=...`; it is not enough to inspect logs or build status. Do not ask for this token unless the user explicitly wants unauthenticated/manual URL build starts.
- Use `.\scripts\jenkins\validate_trigger_token_flow.ps1` for trigger-only dry-run validation. Use `-RequireTriggerToken -Trigger` only when a real unauthenticated URL build launch is intended.
- If Jenkins posts to GitGov, keep `JENKINS_WEBHOOK_SECRET` aligned with the Jenkins shared secret header expected by the backend.
- Jira Cloud API access is configured through ignored env files with `JIRA_BASE_URL`, `JIRA_EMAIL`, `JIRA_API_TOKEN`, and `JIRA_PROJECT_KEY`.
- GitHub webhook authentication is configured through ignored env files with `GITHUB_WEBHOOK_SECRET`; keep it aligned with Render and the GitHub repository webhook.
- Native Jira webhooks require `JIRA_WEBHOOK_SECRET` on Render and the same webhook secret in Jira Cloud.
- Current native Jira webhook name is `GitGov signed issue sync`; it is signed with `JIRA_WEBHOOK_SECRET` and targets `https://gitgov-api.onrender.com/webhooks/jira?org_name=yohandry10`.
- API contract drift reconciliation ticket `KAN-8` records that `docs/ARCHITECTURE.md` is aligned with the real backend routes for `/jobs/{job_id}/retry`, `/compliance/{org_name}`, and `/violations/{violation_id}/decisions`; the remaining contract debt is optional OpenAPI path completeness, not route-table drift. OpenAPI here means the machine-readable API description used by Swagger tools and generated SDKs. If a generated SDK or Swagger contract test becomes a product requirement, implement full OpenAPI annotations deliberately; otherwise use the real backend routes/API directly. `docs/ENTERPRISE_READINESS_DECISION.md` is ignored internal audit memory and must not be force-added.
- `.env.example` placeholder policy ticket `KAN-9` hardens publication safety: real `.env` files remain blocked, `.env.example` remains trackable, and both local `publication_guard.ps1` plus GitHub `Security Guard` validate that sensitive keys in `.env.example` contain placeholder-only values.
- Implementation summary ticket `KAN-10` consolidates the latest closed points and remaining backlog in `docs/IMPLEMENTATION_STATUS.md` and `docs/reports/implementation-progress-summary-2026-04-25.md`.
- API key diagnosis ticket `KAN-11` corrected the manual ingest finding: local ignored `GITGOV_API_KEY` is present and validates against production `/stats` with HTTP `200`. Manual `/integrations/jira` calls also require `x-gitgov-jira-secret` and `org_name` when production `JIRA_WEBHOOK_SECRET` is configured.
- Web publication ticket `KAN-12` records the GitGov marketing/download site push: the invalid local-only commit `f2bdb24` (`dle`) was not pushed, the web changes were recreated on branch `web/KAN-12-web-push`, and the valid traced merge landed on `main` as commit `a0a4174` through PR `#77`.
- Publication governance ticket `KAN-13` clarifies identifier handling: examples/templates must use placeholders, while agent operating memory and historical validation evidence may keep real repo/service identifiers when needed to preserve validation scope.
- Operational validation refresh ticket `KAN-14` records the 2026-04-28 live/local check: Render `/health` was `ok`, production `/stats` returned HTTP `200`, local backend health responded on `http://127.0.0.1:3001/health`, local SonarQube was `UP` with project quality gate `OK`, Jenkins job `gitgov-demo-pipeline` last build `#30` was `SUCCESS`, and release readiness for `main` was `91/100`.
- OpenAPI guard ticket `KAN-15` keeps `/api-docs` honest as a partial schema explorer: `openapi_description_declares_partial_schema_explorer_scope` fails if the OpenAPI description stops pointing operators to `docs/ARCHITECTURE.md` and the `main.rs` route table.
- Provider validation ticket `KAN-16` added `scripts/control-plane/validate_provider_access.ps1`, a secret-safe local smoke check for GitGov production/local health, SonarQube, Jenkins, Jira, and optional release readiness using ignored env files.
- Self-hosted runner runbook ticket `KAN-17` documents the safe path for local SonarQube in GitHub Actions at `docs/runbooks/local-sonar-self-hosted-runner.md`; do not switch required workflows to `runs-on: [self-hosted, gitgov-local-sonar]` until the runner is registered, online, and validated.
- Jenkins trigger-only ticket `KAN-18` added `scripts/jenkins/validate_trigger_token_flow.ps1` and `docs/runbooks/jenkins-trigger-token-flow.md`; the script dry-runs by default, redacts the trigger URL token, and only starts a build when `-Trigger` is explicitly passed.
- Jira traceability ticket `KAN-19` added `scripts/control-plane/validate_jira_traceability_coverage.ps1` and `docs/runbooks/jira-traceability-coverage.md`; latest production validation refreshed correlations and reported ticket coverage `96.67%` (`58/60`) over 720h for `main`.
- Implementation backlog closure ticket `KAN-20` reframed the final status-list items as operational decisions or optional enhancements. No active implementation blocker remains after `KAN-14` through `KAN-20`: Sonar remains local unless a self-hosted runner is added; Jenkins trigger-only token is only needed for unauthenticated build URLs; OpenAPI path completeness is optional unless generated SDK/contract testing is required; traceability coverage stays an operational discipline.
- Operating decision clarification ticket `KAN-21` records three non-negotiable agent defaults: SonarCloud is off the table for this personal repository, OpenAPI/SDK work is optional product work rather than a current blocker, and Jenkins trigger-only token flow is not needed for normal agent operations because authenticated Jenkins API access already exists.
- Current context handoff ticket `KAN-22` added `docs/CURRENT_CONTEXT.md`; use it as the first resume document for state, access, decisions, validators, and next steps. Its commit/PR fields are validated handoff baselines; still run `git status --short --branch` and `git log -1 --oneline main` before new work.
- Workflow invocation follow-up ticket `KAN-22` fixed PowerShell splatting in `risk-tier-baseline-calibration.yml` and `desktop-updater-readiness.yml`: workflow script blocks must use hashtable splatting for named parameters, not array splatting with `"-Param", value` pairs.
- Evidence packets ticket `KAN-23` is the selected first product enhancement after the GPT 5.5 deep research report. PR `#95` merged on `main` as `6d3fb85`, adding ticket-scoped Evidence Packets before a Vercel AI SDK copilot. MVP route is `GET /evidence/packets/tickets/{ticket_id}` with JSON download/hash UI; Vercel AI SDK, MCP, PR/release packet types, and persisted packet export history remain follow-ups. Production validation passed after Render deploy `dep-d7pgh97aqgkc738i2dv0`: `/health` was `ok` and `KAN-23` packet evidence returned complete counters (`1` commit, `1` PR, `1` pipeline, `1` quality gate).
- Product vulnerability review ticket `KAN-24` opened the security hardening stage and merged through PR `#97` as `126167f`. The reusable runner is `scripts/security/run_product_vulnerability_review.ps1`; the master plan is `docs/security/product-vulnerability-review-plan-2026-04-30.md`; the live report is `docs/reports/product-vulnerability-review-2026-04-30.md`; generated evidence lives under `docs/reports/product-vulnerability-review-2026-04-30/`. Latest full runner result was `20` pass, `1` expected finding, `0` fail. No critical/high reachable vulnerability remained open: the residual backend `cargo audit` `rsa` finding is documented as not reachable through inactive `sqlx-mysql`; `rustls-pemfile`, Tauri/GTK ecosystem warnings, and website peer warnings are deferred hygiene. KAN-24 fixed workflow shell-injection patterns, JS/Rust dependency advisories where reachable, Windows URL opener shell wrapping, website contact/download hardening, website security headers, and evidence packet download filename sanitization. Post-merge validation passed on `main` commit `126167f`; Render deploy `dep-d7phm1m8bjmc73fko1lg` reached `live`, `/health` returned `ok`, anonymous `/stats` returned `401`, and authenticated `/stats` returned `200`.
- Product vulnerability automation ticket `KAN-25` operationalizes the KAN-24 runner through `.github/workflows/product-vulnerability-review.yml`. PR `#100` merged on `main` as `7c260fe`. The workflow runs weekly in `DependenciesOnly` mode and manually supports `DependenciesOnly`, `StaticOnly`, `RuntimeSmoke`, and `Full`; artifacts are uploaded as `product-vulnerability-review-{run_id}`. Runbook: `docs/runbooks/product-vulnerability-review-automation.md`. Report: `docs/reports/product-vulnerability-review-automation-2026-04-30.md`. The runner was made cross-platform for GitHub-hosted Ubuntu while preserving local Windows use. First manual validation run `25157972836` passed on `main`, uploaded artifact `product-vulnerability-review-25157972836`, and the artifact was not expired.
- Product vulnerability artifact monitor ticket `KAN-26` adds `.github/workflows/product-vulnerability-review-artifact-monitor.yml` to validate freshness of artifacts produced by `product-vulnerability-review.yml`. PR `#102` merged on `main` as `89a234c`. The shared artifact validator `scripts/control-plane/validate_github_evidence_report_artifact.ps1` now supports `-ArtifactNamePrefix`, used with prefix `product-vulnerability-review-`. Report: `docs/reports/product-vulnerability-review-artifact-monitor-2026-04-30.md`. First manual monitor run `25158436168` passed and uploaded artifact `product-vulnerability-review-artifact-monitor` ID `6727075935`, not expired.
- Product vulnerability trend ticket `KAN-27` adds `.github/workflows/product-vulnerability-review-trend-report.yml` and `scripts/control-plane/generate_product_vulnerability_review_trend_report.ps1` to build multi-run trend evidence from sanitized Product Vulnerability Review artifacts. PR `#104` merged on `main` as `6fd8de8`. Report: `docs/reports/product-vulnerability-review-trend-2026-04-30.md`. Local validation analyzed run `25157972836` and artifact `product-vulnerability-review-25157972836`, producing trend status `findings` with `5` pass, `1` expected finding, and `0` fail. First manual trend workflow validation passed on run `25159031614`, uploaded artifact `product-vulnerability-review-trend-report` ID `6727320469`, and post-merge `CI` run `25159025219` plus `Release Readiness Gate` run `25159025186` passed.
- Product roadmap after the security review is documented in `docs/design/enterprise-self-service-and-ai-copilot-roadmap.md`: the next product features are Enterprise Self-Service Adoption and Vercel AI SDK Copilot; KAN-28 is the completed hardening gate before those larger product builds.
- Product vulnerability trend enforcement ticket `KAN-28` adds `.github/workflows/product-vulnerability-review-trend-enforcement.yml` and enforcement switches to `scripts/control-plane/generate_product_vulnerability_review_trend_report.ps1`. PR `#106` merged on `main` as `7b36cec`. It fails when latest failures exceed `0`, latest findings exceed the accepted baseline of `1`, findings/failures increase, or the latest successful review run lacks a parseable artifact. Local enforcement validation against run `25157972836` passed with `5` pass, `1` expected finding, and `0` fail. First manual enforcement workflow validation passed on run `25160194313`, uploaded artifact `product-vulnerability-review-trend-enforcement` ID `6727810243`, and post-merge `CI` run `25160187848` plus `Release Readiness Gate` run `25160187829` passed.
- Enterprise Self-Service Adoption ticket `KAN-29` starts the first product packaging MVP after the security review. PR `#108` merged on `main` as `bf8e378`. It adds `scripts/control-plane/generate_enterprise_adoption_pack.ps1`, `docs/design/enterprise-self-service-adoption-mvp.md`, `docs/runbooks/enterprise-self-service-adoption.md`, and `docs/examples/enterprise-adoption-profile.example.json`. The generator produces Markdown/JSON adoption packs with modules, providers, policy preset, workflow plan, variable/secret names, and manual setup checklist without reading or writing secret values. Local validation generated the ExampleCo pack successfully; post-merge `CI` run `25160842461` and `Release Readiness Gate` run `25160842032` passed.
- Adoption Profile Dashboard ticket `KAN-30` continues Enterprise Self-Service Adoption by adding the first admin UI for the KAN-29 profile shape. PR `#110` merged on `main` as `0412574`. It adds `gitgov/src/components/control_plane/EnterpriseAdoptionPanel.tsx`, typed adoption pack helpers in `dashboard-helpers.ts`, validation tests, `docs/design/adoption-profile-dashboard-mvp.md`, and `docs/reports/adoption-profile-dashboard-mvp-2026-04-30.md`. The dashboard JSON export remains secret-safe: it contains variable/secret names only and does not read local env files or provider tokens. Post-merge validation passed with `CI` run `25161644820` and `Release Readiness Gate` run `25161644879`.
- Adoption Profile Persistence ticket `KAN-31` continues Enterprise Self-Service Adoption by persisting the KAN-30 profile per organization. PR `#112` merged on `main` as `509e2a2`. It adds admin routes `GET/PUT /enterprise/adoption-profile`, backend JSON validation, Supabase migration `supabase_schema_v23.sql`, Tauri commands, dashboard save/load controls, `docs/design/adoption-profile-persistence-mvp.md`, and `docs/reports/adoption-profile-persistence-2026-04-30.md`. The saved profile remains secret-safe: it stores provider/module intent, policy preset, repository/default-branch/Jira key, and no token or `.env` values. Post-merge validation passed with `CI` run `25186881414` and `Release Readiness Gate` run `25186881375`; docs refresh PR `#113` merged as `171d43d` with post-merge `CI` run `25187583892` and `Release Readiness Gate` run `25187583994` passing. Production DB migration `v23` was applied on 2026-04-30, postcheck passed, `/health` returned `200`, anonymous `GET /enterprise/adoption-profile?org_name=yohandry10` returned `401`, and authenticated GET returned `200` with `found=false`.
- Provider Health Validation ticket `KAN-32` continues Enterprise Self-Service Adoption by adding a secret-safe Provider Health section to the adoption dashboard. PR `#115` merged on `main` as `1a16d88`. It evaluates selected providers from the persisted/profile draft plus already-loaded GitGov evidence: GitHub event totals, Jira ticket coverage, Jenkins pipeline runs, Sonar/quality runs inferred from Jenkins correlations, and active repo count. Status values are `ready`, `needs-evidence`, and `needs-config`. It does not read `.env`, provider tokens, or raw secret values, and it does not call external provider APIs directly. Post-merge validation passed with `CI` run `25188414404` and `Release Readiness Gate` run `25188414418`.
- Workflow Template Generation ticket `KAN-33` continues Enterprise Self-Service Adoption by converting the adoption profile into reviewed GitHub Actions workflow template packs. PR `#117` merged on `main` as `62b67e5`. It adds `scripts/control-plane/generate_enterprise_workflow_templates.ps1`, `docs/design/workflow-template-generation-mvp.md`, and `docs/reports/workflow-template-generation-2026-04-30.md`. The generator writes ignored output under `out/enterprise-workflow-templates/` with `README.md`, `workflow-template-manifest.json`, and selected `.github/workflows/*.yml`; the ExampleCo profile generates `13` templates. It does not read `.env`, provider tokens, or secret values, and it does not mutate customer repositories automatically. Workflow installation remains a reviewed/manual or future explicitly authorized step. Post-merge validation passed with `CI` run `25189490341` and `Release Readiness Gate` run `25189490316`.
- Dashboard Workflow Template Pack ticket `KAN-34` continues Enterprise Self-Service Adoption by exposing workflow template pack download inside the Enterprise Adoption dashboard. PR `#119` merged on `main` as `31b109d`. It adds `buildEnterpriseWorkflowTemplatePack`, `buildEnterpriseWorkflowTemplatePackFilename`, a `Workflows` download action in `EnterpriseAdoptionPanel.tsx`, focused helper tests, `docs/design/dashboard-workflow-template-pack-mvp.md`, and `docs/reports/dashboard-workflow-template-pack-2026-04-30.md`. The dashboard pack is a single JSON file with manifest, README text, and workflow file contents. It does not read `.env`, does not read/print secret values, and does not mutate customer repositories. Vercel AI SDK Copilot later starts in `KAN-38` after Enterprise Self-Service Onboarding is more complete. Post-merge validation passed with `CI` run `25190963652` and `Release Readiness Gate` run `25190963636`.
- Reviewed Workflow Installation ticket `KAN-35` continues Enterprise Self-Service Adoption by adding a dry-run-first local installer for workflow template packs. PR `#121` merged on `main` as `c60c486`. It adds `scripts/control-plane/install_enterprise_workflow_templates.ps1`, `docs/design/reviewed-workflow-installation-mvp.md`, and `docs/reports/reviewed-workflow-installation-2026-04-30.md`. The installer accepts CLI `-PackDir` output or dashboard `-PackPath` JSON, writes only under `.github/workflows`, requires explicit `-Apply`, requires `-Overwrite` for replacements, rejects unsafe paths, and does not read `.env`, provider tokens, or secret values. It does not mutate remote GitHub repositories. Post-merge validation passed with `CI` run `25191857023` and `Release Readiness Gate` run `25191857006`. Direct provider checks are covered by `KAN-36`, formal enterprise release approval is covered by `KAN-37`, and Vercel AI SDK Copilot later starts in `KAN-38`.
- Provider Connection Validation ticket `KAN-36` continues Enterprise Self-Service Adoption by adding secret-safe direct provider credential/reachability checks for onboarding. PR `#123` merged on `main` as `8c075a4`. It adds `scripts/control-plane/validate_enterprise_provider_connections.ps1`, `docs/design/provider-connection-validation-mvp.md`, and `docs/reports/provider-connection-validation-2026-04-30.md`. The validator supports GitHub, Jira, Jenkins, SonarQube, Render, and Vercel; reads credentials only from ignored env files or process environment; reports required config names and sanitized statuses; and does not print secret values, mutate provider state, create webhooks, create GitHub Actions variables/secrets, or mutate customer repositories. Post-merge validation passed with `CI` run `25192626074` and `Release Readiness Gate` run `25192626059`. Formal enterprise release approval is covered by `KAN-37`, and Vercel AI SDK Copilot later starts in `KAN-38`.
- Formal Release Approval ticket `KAN-37` completes the first Enterprise Self-Service formal approval MVP. PR `#125` merged on `main` as `d7ae92e`; Jira final comment is `10196`. It adds `GET /enterprise/release-approvals`, `POST /enterprise/release-approvals`, `enterprise_release_approvals`, Supabase migration `v24`, evidence packet hash binding, risk acceptance expiration, admin-only org scope, stale-auth-cache blocking for this sensitive admin route, and audit logging. Design: `docs/design/formal-release-approval-mvp.md`. Report: `docs/reports/formal-release-approval-2026-04-30.md`. Local backend validation passed with `cargo test`, `cargo check`, `cargo clippy -- -D warnings`, `git diff --check`, and `.\scripts\security\publication_guard.ps1`. Production DB migration `v24` was applied and postchecked; Render deploy `dep-d7ptsvhoagis738cj88g` reached `live`; production smoke validated `/health`, anonymous `401`, Evidence Packet lookup for `KAN-37`, and creation/listing of `KAN-37-runtime-smoke`. Vercel AI SDK Copilot starts in `KAN-38`.
- Vercel AI SDK Governance Copilot ticket `KAN-38` completes the first AI copilot MVP. PR `#127` merged on `main` as `9742472`; Jira final comment is `10197`. It adds `ai@^6.0.0` to `gitgov-web`, server-side route `POST /api/copilot/governance`, and helper module `gitgov-web/lib/copilot/governance.ts`. The route gathers Evidence Packet, Jira ticket coverage, formal release approval, and adoption profile evidence from GitGov, then uses Vercel AI SDK `generateText()` when AI Gateway/OIDC is available; otherwise it returns a deterministic evidence brief. Design: `docs/design/ai-sdk-governance-copilot-mvp.md`. Report: `docs/reports/ai-sdk-governance-copilot-2026-04-30.md`. Security defaults require caller Bearer auth unless explicit token-gated server-key mode is configured; no Authorization headers or provider secrets are logged or returned. Local validation passed with `pnpm run typecheck`, `pnpm run lint`, `pnpm run build`, `pnpm audit --prod`, local route smoke with `mode=fallback`, `git diff --check`, and `.\scripts\security\publication_guard.ps1`. Post-merge validation passed with `CI` run `25194421718` and `Release Readiness Gate` run `25194421743`; Vercel production deployment `https://git-ih2bzdqq5-trivia1.vercel.app` reached `Ready`; production route smoke passed on `https://www.gitgov.cloud/api/copilot/governance` and `https://git-gov.vercel.app/api/copilot/governance` with `success=true`, `mode=fallback`, `4` citations, `4` sources, and `1` expected warning because production AI Gateway/OIDC generation was not active.
- Governance Copilot Dashboard ticket `KAN-39` completes the first operator UI for the KAN-38 copilot route. PR `#129` merged on `main` as `eda2f13`; Jira final comment is `10198`. It adds Tauri command `cmd_server_governance_copilot_ask`, dashboard store action `askGovernanceCopilot`, and admin dashboard component `GovernanceCopilotPanel`. The dashboard browser does not call the public copilot endpoint directly; the desktop command forwards the configured GitGov API key only as a Bearer token to `https://www.gitgov.cloud/api/copilot/governance` unless process env `GITGOV_COPILOT_URL` is explicitly set to an allowlisted GitGov/Vercel or loopback URL without embedded credentials. Design: `docs/design/governance-copilot-dashboard-mvp.md`. Report: `docs/reports/governance-copilot-dashboard-2026-04-30.md`. Local validation passed with `cargo fmt`, `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm test -- --run src/test/useControlPlaneStore.test.ts`, full `npm test -- --run`, `npm run typecheck`, `npm run lint`, `npm run build`, local Vite smoke `GET http://127.0.0.1:5174/`, `git diff --check`, and `.\scripts\security\publication_guard.ps1`. Post-merge validation passed with `CI` run `25195469511` and `Release Readiness Gate` run `25195469482`.
- Governance Copilot AI mode validation ticket `KAN-40` starts the operational activation step after KAN-38/KAN-39. PR `#131` merged on `main` as `2b507bc`. It adds `scripts/control-plane/validate_governance_copilot_ai_mode.ps1`, `.github/workflows/governance-copilot-ai-mode-validation.yml`, `docs/runbooks/governance-copilot-ai-mode-validation.md`, and `docs/reports/governance-copilot-ai-mode-validation-2026-04-30.md`. The validator is secret-safe: it calls the copilot route with `GITGOV_API_KEY`, records mode/source/citation counts, stores only answer length and SHA-256 hash, and supports `-RequireAiMode` for post-AI-Gateway/OIDC enforcement. Local production validation passed in non-strict `fallback` mode with HTTP `200`, `4` citations, `4` sources, and `4` ok sources; strict mode currently fails as expected because production AI Gateway/OIDC is not active. Post-merge `main` checks passed with `CI` run `25196003313` and `Release Readiness Gate` run `25196003326`; first manual workflow validation passed on run `25196010712` and uploaded artifact `governance-copilot-ai-mode-validation` ID `6742816838`.
- Google AI SDK Copilot Provider ticket `KAN-41` switches the practical production AI activation path from Vercel AI Gateway to direct Google Gemini through `@ai-sdk/google`, because Vercel AI Gateway generation required a billing card and the project already has a Gemini integration. PRs `#133`, `#134`, `#135`, and `#136` merged on `main` through final implementation commit `ba61d16`. Vercel production env now has `GOOGLE_GENERATIVE_AI_API_KEY`, `GITGOV_COPILOT_PROVIDER=google`, and `GITGOV_COPILOT_GOOGLE_MODEL=gemini-2.5-flash` configured without printing secret values; preview remains fallback-only unless explicitly configured later. The initial Vercel key upload was corrected after sanitized validation showed an invisible BOM and then an expired first local key; the production secret was reconfigured from the effective local Gemini key and redeployed as `https://git-8gwowu155-trivia1.vercel.app`. Local validation passed with `pnpm run typecheck`, `pnpm run lint`, `pnpm run build`, `pnpm audit --prod`, `git diff --check`, `.\scripts\security\publication_guard.ps1`, and a local route smoke returning HTTP `200`, `success=true`, `mode=ai`, `model=google/gemini-2.5-flash`, `4` citations, `4` sources, `4` ok sources, and `0` warnings. Production strict validation passed with `.\scripts\control-plane\validate_governance_copilot_ai_mode.ps1 -RequireAiMode`: HTTP `200`, `success=true`, `mode=ai`, `model=google/gemini-2.5-flash`, `4` citations, `4` sources, `4` ok sources, and `0` warnings. Report: `docs/reports/google-ai-sdk-copilot-provider-2026-05-01.md`.
- Governance Copilot AI mode enforcement ticket `KAN-42` makes the KAN-40 validator strict by default now that KAN-41 production AI mode is active. PR `#138` merged on `main` as `7ad1c9d`. It updates `.github/workflows/governance-copilot-ai-mode-validation.yml` so scheduled runs require `mode=ai`, manual dispatch defaults to `require_ai_mode=true`, manual `require_ai_mode=false` remains available for diagnostics, and missing `GITGOV_API_KEY` fails strict runs instead of silently skipping. Report: `docs/reports/governance-copilot-ai-mode-enforcement-2026-05-01.md`. Local validation parsed the workflow YAML and strict production validation passed with HTTP `200`, `success=true`, `mode=ai`, `model=google/gemini-2.5-flash`, `4` citations, `4` sources, `4` ok sources, and `0` warnings. Post-merge checks passed with `CI` run `25200079701` and `Release Readiness Gate` run `25200079686`; first strict manual workflow validation on `main` passed on run `25200126845` and uploaded artifact `governance-copilot-ai-mode-validation` ID `6744359123`.
- Release Approval Dashboard ticket `KAN-43` adds the first admin UI for formal release approvals on top of the KAN-37 backend API. PR `#140` merged on `main` as `10d0c4b`. It adds `gitgov/src/components/control_plane/ReleaseApprovalPanel.tsx`, Zustand release approval list/create actions, Tauri client structs/methods/commands for `GET/POST /enterprise/release-approvals`, and docs `docs/design/release-approval-dashboard-mvp.md` plus `docs/reports/release-approval-dashboard-2026-05-01.md`. The UI requires explicit operator confirmation, validates evidence hashes, decision/risk combinations, accepted-risk reason/expiration, ticket shape, SHA shape, and evidence URI shape before submit. Local validation passed with `cargo fmt`, `cargo check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run lint`, `npm run typecheck`, `npm test -- --run`, and `npm run build`. Post-merge validation passed with `CI` run `25202577666`, `Release Readiness Gate` run `25202577665`, `Secret Scan` run `25202577668`, `Public Naming Guard` run `25202577675`, `Quality Gate Policy Matrix (Optional)` run `25202577671`, `SonarQube Governance (Non-Blocking)` run `25202577669`, `Governance Correlation Smoke (Optional)` run `25202577688`, and `Desktop Updater Readiness (Optional)` run `25202577680`.
- Configurable Release Governance Defaults ticket `KAN-44` records the product rule that future multi-approver quorum and release-blocking enforcement are customer-configurable opt-in capabilities, not GitGov defaults. PR `#142` merged on `main` as `eb15b08`. Default release approval behavior remains `record-only`: approval records and evidence can be saved and reported, but pipelines are not failed and multiple approvers are not required unless customer policy explicitly enables those modes. Design: `docs/design/configurable-release-governance-defaults.md`. Report: `docs/reports/configurable-release-governance-defaults-2026-05-01.md`. Local validation passed with `git diff --check` and `.\scripts\security\publication_guard.ps1`. Post-merge validation passed with `CI` run `25203116708`, `Release Readiness Gate` run `25203116684`, `Secret Scan` run `25203116635`, `Public Naming Guard` run `25203116673`, `Quality Gate Policy Matrix (Optional)` run `25203116644`, `SonarQube Governance (Non-Blocking)` run `25203116668`, `Governance Correlation Smoke (Optional)` run `25203116650`, and `Desktop Updater Readiness (Optional)` run `25203116657`.
- Release Governance Profile Policy ticket `KAN-45` turns the KAN-44 decision into explicit adoption-profile configuration. PR `#144` merged on `main` as `dc37e92`. It adds default `record-only` / `disabled` release governance, optional `advisory`, `approval-required`, and `quorum-required` modes, dashboard controls, backend profile validation, adoption pack output, workflow template manifest output, and docs `docs/design/release-governance-profile-policy-mvp.md` plus `docs/reports/release-governance-profile-policy-2026-05-01.md`. The default remains non-blocking; non-`record-only` modes require the `formal-approval` module. No provider tokens, `.env` values, Authorization headers, or customer secret values are read or written. Post-merge validation passed with `CI` run `25203785504`, `Release Readiness Gate` run `25203785499`, `Secret Scan` run `25203785497`, `Public Naming Guard` run `25203785483`, `Quality Gate Policy Matrix (Optional)` run `25203785520`, `SonarQube Governance (Non-Blocking)` run `25203785527`, `Governance Correlation Smoke (Optional)` run `25203785490`, and `Desktop Updater Readiness (Optional)` run `25203785503`.
- Release Governance Evaluator ticket `KAN-46` adds the first evaluator for customer-selected release governance policy. PR `#146` merged on `main` as `0252432`. It adds admin endpoint `GET /enterprise/release-governance/evaluate`, Tauri command `cmd_server_evaluate_enterprise_release_governance`, dashboard evaluation UI in `ReleaseApprovalPanel`, approver role metadata through `evidence_summary.approver_role`, and docs `docs/design/release-governance-evaluator-mvp.md` plus `docs/reports/release-governance-evaluator-2026-05-01.md`. The evaluator reads the KAN-45 profile policy and KAN-37 approval records, returns `recorded`, `advisory-warning`, `approved`, `would-block`, or `blocked`, and keeps `record-only` non-blocking by default. Post-merge checks passed with `CI` run `25207328590` and `Release Readiness Gate` run `25207328587`; Render deploy `dep-d7q5qmkvikkc73cmfg0g` reached `live`, `/health` returned `ok`, anonymous evaluator access returned `401`, and authenticated evaluator smoke returned `200` with `status=recorded`, `policy_mode=record-only`, `blocking=false`, `would_block=false`, `valid=0`, and `required=0`. No provider tokens, `.env` values, Authorization headers, or customer secret values are read or written.
- Release Governance Enforcement Gate ticket `KAN-47` completed the optional release governance enforcement gate. PR `#148` merged on `main` as `b6b2854`. It adds `scripts/control-plane/validate_release_governance_gate.ps1`, manual workflow `.github/workflows/release-governance-gate.yml`, CLI and dashboard workflow-template-pack support for opt-in release governance gate generation, runbook `docs/runbooks/release-governance-gate.md`, design `docs/design/release-governance-enforcement-gate-mvp.md`, and report `docs/reports/release-governance-enforcement-gate-2026-05-01.md`. The gate is report-only by default; `-Enforce` fails only when KAN-46 returns `blocking=true`; generated customer gate templates appear only for `formal-approval` plus non-`record-only` release governance. Local validation passed with release governance script report-only/enforce smoke, record-only and quorum template generation, YAML parse checks, dashboard helper tests, full frontend tests, lint/typecheck/build, `git diff --check`, and `.\scripts\security\publication_guard.ps1`. Post-merge `CI` run `25208426343` and `Release Readiness Gate` run `25208426384` passed. First manual `Release Governance Gate` workflow run `25208470238` passed in non-blocking mode and uploaded artifact `release-governance-gate-25208470238` ID `6747272652`, not expired.
- Environment-Scoped Release Governance Policy ticket `KAN-48` completed optional `release_governance.environment_overrides`. PR `#150` merged on `main` as `cba3f9d`. A customer can keep the base profile `record-only` while explicitly making a selected environment such as `production` advisory, approval-required, or quorum-required. Design: `docs/design/environment-scoped-release-governance-policy-mvp.md`. Report: `docs/reports/environment-scoped-release-governance-policy-2026-05-01.md`. No database migration is required; profile JSON validation, evaluator policy resolution, dashboard helpers/UI, CLI adoption pack generation, and workflow template generation are the implementation scope. Default blocking remains off unless the customer explicitly enables a non-`record-only` policy and the `formal-approval` module. Local validation passed with backend `cargo check`, `cargo clippy -- -D warnings`, full backend `cargo test` (`189` tests), frontend typecheck/lint/full tests/build (`285` tests), override profile pack/template generation, `git diff --check`, and `.\scripts\security\publication_guard.ps1`. Post-merge `CI` run `25209198316` and `Release Readiness Gate` run `25209198277` passed.
- Release Governance Gate Artifact Monitor ticket `KAN-49` completed opt-in freshness monitoring for artifacts emitted by `.github/workflows/release-governance-gate.yml`. PR `#152` merged on `main` as `4257a95`. It adds `.github/workflows/release-governance-gate-artifact-monitor.yml`, reuses `scripts/control-plane/validate_github_evidence_report_artifact.ps1` with prefix `release-governance-gate-`, and adds CLI/dashboard enterprise template support only when `formal-approval`, non-`record-only` release governance, and `artifact-monitoring` are all selected. Design: `docs/design/release-governance-gate-artifact-monitor-mvp.md`. Report: `docs/reports/release-governance-gate-artifact-monitor-2026-05-01.md`. Runbook: `docs/runbooks/release-governance-gate.md`. Local validation passed with artifact monitor smoke against run `25208470238`, CLI template generation, frontend tests/typecheck/lint/build, `git diff --check`, and `.\scripts\security\publication_guard.ps1`. Post-merge `CI` run `25209672506` and `Release Readiness Gate` run `25209672484` passed. First manual monitor workflow run `25209735562` passed and uploaded artifact `release-governance-gate-artifact-monitor` ID `6747717581`.
- Remote Workflow Installation PR ticket `KAN-50` completes the first remote PR-based workflow installation path for Enterprise Self-Service Adoption. PR `#154` merged on `main` as `eb7482b`. It adds `scripts/control-plane/open_enterprise_workflow_template_pr.ps1`, supporting CLI `-PackDir` and dashboard-style `-PackPath` sources, remote compare against GitHub base branch, dry-run by default, explicit `-Apply` before branch/commit/draft PR creation, and explicit `-Overwrite` before replacing differing workflow files. Design: `docs/design/remote-workflow-installation-pr-mvp.md`. Report: `docs/reports/remote-workflow-installation-pr-2026-05-01.md`. Runbook: `docs/runbooks/enterprise-self-service-adoption.md`. Local validation passed with PackDir dry-run, PackDir overwrite dry-run, minimal PackPath dry-run, PowerShell parse check, dry-run plan secret scan, `git diff --check`, and `.\scripts\security\publication_guard.ps1`. Post-merge `CI` run `25210329452` and `Release Readiness Gate` run `25210329443` passed. No real remote apply was run against GitGov; the feature remains customer/operator opt-in.
- Remote Workflow Readiness Validation ticket `KAN-51` completes read-only validation after local/remote workflow installation. PR `#156` merged on `main` as `dcfb529`. It adds `scripts/control-plane/validate_enterprise_workflow_installation_readiness.ps1`, supporting CLI `-PackDir` and dashboard-style `-PackPath`, expected workflow file presence/content-hash checks, GitHub Actions variable-name checks, and GitHub Actions secret-name checks without reading secret values. Design: `docs/design/remote-workflow-readiness-validation-mvp.md`. Report: `docs/reports/remote-workflow-readiness-validation-2026-05-01.md`. Runbook: `docs/runbooks/enterprise-self-service-adoption.md`. Local validation passed with ExampleCo pack generation, PackDir report-only readiness against GitGov returning expected `needs-action`, minimal PackPath report-only readiness returning expected `needs-action`, output secret/header scan, `git diff --check`, and `.\scripts\security\publication_guard.ps1`. Post-merge `CI` run `25210718116` and `Release Readiness Gate` run `25210718113` passed. No remote mutation or secret value access occurs in this feature.
- Enterprise Onboarding Readiness Report ticket `KAN-52` consolidates the self-service adoption state into one customer-facing readiness snapshot. PR `#158` merged on `main` as `268770a`. It adds `scripts/control-plane/generate_enterprise_onboarding_readiness_report.ps1`, dashboard helper `buildEnterpriseOnboardingReadinessReport`, and a `Readiness` JSON export in `EnterpriseAdoptionPanel`. Design: `docs/design/enterprise-onboarding-readiness-report-mvp.md`. Report: `docs/reports/enterprise-onboarding-readiness-report-2026-05-01.md`. Runbook: `docs/runbooks/enterprise-self-service-adoption.md`. Local validation passed with PowerShell parser check, focused dashboard helper tests, full frontend tests, typecheck, lint, build, profile-only readiness generation (`needs-action`, score `75`), provider/workflow-input readiness generation (`needs-action`, score `83`), generated-output secret scan, `git diff --check`, and `.\scripts\security\publication_guard.ps1`. Post-merge `CI` run `25211254174` and `Release Readiness Gate` run `25211254160` passed. The report is read-only, checks secret/config names only, and does not mutate customer repositories, providers, branch protection, workflows, variables, or secrets.
- Enterprise Onboarding Readiness Automation ticket `KAN-53` operationalizes the KAN-52 report through `.github/workflows/enterprise-onboarding-readiness.yml`. PR `#160` merged on `main` as `027a10f`. The workflow runs manually and weekly, generates a temporary adoption profile with safe defaults, can optionally run KAN-51 remote workflow readiness in read-only report-only mode, and uploads `enterprise-onboarding-readiness-{run_id}`. Design: `docs/design/enterprise-onboarding-readiness-automation-mvp.md`. Report: `docs/reports/enterprise-onboarding-readiness-automation-2026-05-01.md`. Runbook: `docs/runbooks/enterprise-self-service-adoption.md`. Local validation passed with GitGov adoption pack generation, workflow template generation, KAN-52 default readiness (`needs-action`, score `75`), optional KAN-51 remote readiness (`needs-action`, expected `13` different workflow files and `1` missing secret name), generated-output secret scan, `git diff --check`, and `.\scripts\security\publication_guard.ps1`. Post-merge `CI` run `25211635818` and `Release Readiness Gate` run `25211635807` passed. First manual workflow run `25211644692` passed and uploaded artifact `enterprise-onboarding-readiness-25211644692`, ID `6748421926`, not expired.
- Enterprise Onboarding Readiness Artifact Monitor ticket `KAN-54` adds `.github/workflows/enterprise-onboarding-readiness-artifact-monitor.yml` to validate that the latest successful KAN-53 workflow uploaded a fresh `enterprise-onboarding-readiness-` artifact. PR `#162` merged on `main` as `ec99b7c`. Design: `docs/design/enterprise-onboarding-readiness-artifact-monitor-mvp.md`. Report: `docs/reports/enterprise-onboarding-readiness-artifact-monitor-2026-05-01.md`. Runbook: `docs/runbooks/enterprise-self-service-adoption.md`. Local validation using `scripts/control-plane/validate_github_evidence_report_artifact.ps1` passed against run `25211644692` and artifact `enterprise-onboarding-readiness-25211644692`, ID `6748421926`; `git diff --check` and `.\scripts\security\publication_guard.ps1` passed. Post-merge `CI` run `25212018101` and `Release Readiness Gate` run `25212018102` passed. First manual monitor workflow run `25212021793` passed and uploaded artifact `enterprise-onboarding-readiness-artifact-monitor`, ID `6748551922`, not expired. The monitor reads GitHub artifact metadata only, does not read `.env` or provider tokens, does not mutate customer repositories/providers, and does not make onboarding readiness or release governance blocking by default.
- Enterprise Onboarding Readiness Trend ticket `KAN-55` adds `scripts/control-plane/generate_enterprise_onboarding_readiness_trend_report.ps1` and `.github/workflows/enterprise-onboarding-readiness-trend-report.yml` to parse recent KAN-53 readiness artifacts and report latest status, readiness score, stage counts, and deltas. PR `#164` merged on `main` as `e5c259d`. Design: `docs/design/enterprise-onboarding-readiness-trend-mvp.md`. Report: `docs/reports/enterprise-onboarding-readiness-trend-2026-05-01.md`. Runbook: `docs/runbooks/enterprise-self-service-adoption.md`. Local validation parsed KAN-53 run `25211644692` and artifact `enterprise-onboarding-readiness-25211644692`, producing latest status `needs-action`, score `75`, `3` ready stages, `3` needs-action stages, `0` blocked stages, and trend direction `stable`; `git diff --check` and `.\scripts\security\publication_guard.ps1` passed. Post-merge `CI` run `25212383270` and `Release Readiness Gate` run `25212383263` passed. First manual trend workflow run `25212387234` passed and uploaded artifact `enterprise-onboarding-readiness-trend-report`, ID `6748686954`, not expired. The trend reads GitHub artifact metadata and sanitized readiness JSON only, does not read `.env` or provider tokens, does not mutate customer repositories/providers, and does not make onboarding readiness or release governance blocking by default.
- Enterprise Onboarding Readiness Trend Monitor ticket `KAN-56` adds `scripts/control-plane/validate_enterprise_onboarding_readiness_trend_monitor.ps1` and `.github/workflows/enterprise-onboarding-readiness-trend-monitor.yml` to validate KAN-55 trend evidence freshness and detect onboarding readiness deterioration. PR `#166` merged on `main` as `89175b3`. Design: `docs/design/enterprise-onboarding-readiness-trend-monitor-mvp.md`. Report: `docs/reports/enterprise-onboarding-readiness-trend-monitor-2026-05-01.md`. Runbook: `docs/runbooks/enterprise-self-service-adoption.md`. Local validation against trend run `25212387234` and artifact `enterprise-onboarding-readiness-trend-report` returned monitor status `ready`, latest readiness status `needs-action`, score `75`, trend `stable`, `0` blocked stages, and `0` findings; strict mode also exited `0` because the monitor status was `ready`; `git diff --check` and `.\scripts\security\publication_guard.ps1` passed. Post-merge `CI` run `25212797552` and `Release Readiness Gate` run `25212797547` passed. First manual monitor workflow run `25212805979` passed and uploaded artifact `enterprise-onboarding-readiness-trend-monitor`, ID `6748834779`, not expired. The monitor reads GitHub artifact metadata and sanitized trend JSON only, does not read `.env` or provider tokens, does not mutate customer repositories/providers, and remains report-only by default unless an operator explicitly disables report-only mode.
- Enterprise Onboarding Remediation Plan ticket `KAN-57` adds `scripts/control-plane/generate_enterprise_onboarding_remediation_plan.ps1` to convert KAN-52 readiness JSON into prioritized owner/action/validation remediation steps plus placeholder-only GitHub Actions variable/secret setup commands. PR `#168` merged on `main` as `dca7e0b`. Design: `docs/design/enterprise-onboarding-remediation-plan-mvp.md`. Report: `docs/reports/enterprise-onboarding-remediation-plan-2026-05-02.md`. Runbook: `docs/runbooks/enterprise-self-service-adoption.md`. Local validation generated ExampleCo readiness (`needs-action`, score `75`, `3` ready, `3` needs-action, `0` blocked), then generated a remediation plan with `3` actions, `3` variable names, and `2` secret names using placeholder-only commands; generated output secret-pattern scan, `git diff --check`, and `.\scripts\security\publication_guard.ps1` passed. Post-merge `CI` run `25243574261` and `Release Readiness Gate` run `25243574245` passed. The plan reads readiness/adoption pack JSON only, does not read `.env` or provider tokens, does not create variables/secrets, does not mutate customer repositories/providers, and remains advisory/non-blocking by default.
- Dashboard Onboarding Remediation Export ticket `KAN-58` exposes the KAN-57 remediation plan in the Enterprise Adoption dashboard. PR `#170` merged on `main` as `4f0eff5`. It adds `buildEnterpriseOnboardingRemediationPlan`, `buildEnterpriseOnboardingRemediationPlanFilename`, a dashboard `Plan` JSON download action, focused helper tests, design `docs/design/dashboard-onboarding-remediation-export-mvp.md`, and report `docs/reports/dashboard-onboarding-remediation-export-2026-05-02.md`. Local validation passed with focused dashboard helper tests (`24` tests), frontend typecheck/lint/full tests/build (`292` tests), `git diff --check`, and `.\scripts\security\publication_guard.ps1`. Post-merge `CI` run `25243856927` and `Release Readiness Gate` run `25243856920` passed. The dashboard plan is read-only and secret-safe: it does not read `.env`, provider tokens, or secret values; it lists variable/secret names with `<value>` placeholders only; it does not create variables/secrets, mutate customer repositories/providers, dispatch workflows, change branch protection, or make release blocking default.
- Dashboard Guided Onboarding Checklist ticket `KAN-59` turns the KAN-58 remediation data into a visible checklist in the Enterprise Adoption dashboard. PR `#172` merged on `main` as `d2ce33b`. It adds `buildEnterpriseOnboardingGuide`, guide step/status types, a `Guided checklist` dashboard section, focused helper tests, design `docs/design/dashboard-guided-onboarding-checklist-mvp.md`, and report `docs/reports/dashboard-guided-onboarding-checklist-2026-05-02.md`. Local validation passed with focused helper tests (`26` tests), frontend typecheck/lint/full tests/build (`294` tests), `git diff --check`, and `.\scripts\security\publication_guard.ps1`. Post-merge `CI` run `25244188759` and `Release Readiness Gate` run `25244188770` passed. The checklist is read-only and secret-safe: it does not read `.env`, provider tokens, or secret values; it does not call provider APIs, create variables/secrets, mutate customer repositories/providers, dispatch workflows, change branch protection, or make release blocking default.
- Guided Onboarding Checklist Tracking ticket `KAN-60` persists human/operator tracking metadata for the guided Enterprise Adoption checklist. PR `#174` merged on `main` as `5ebbfa1`. It adds admin `GET/PUT /enterprise/onboarding-checklist-tracking`, table `enterprise_onboarding_checklist_tracking`, Supabase migration `v25`, stale-auth-cache fail-closed handling for the new sensitive admin route, Tauri commands, dashboard load/save actions, and per-stage tracking controls for status, owner, target date, external reference, and notes. Design: `docs/design/guided-onboarding-checklist-tracking-mvp.md`. Report: `docs/reports/guided-onboarding-checklist-tracking-2026-05-02.md`. Local validation passed with backend `cargo check`, `cargo clippy -- -D warnings`, `cargo test` (`192` tests), Tauri `cargo check`, `cargo clippy -- -D warnings`, `cargo test` (`23` tests), frontend helper tests (`28` tests), full frontend tests (`296` tests), typecheck/lint/build, `git diff --check`, and `.\scripts\security\publication_guard.ps1`. Post-merge `CI` run `25244715786` and `Release Readiness Gate` run `25244715777` passed. Production DB migration `v25` was applied, `v25_postcheck.sql` passed, Render deploy `dep-d7qol80k1i2s73dpedag` reached `live`, `/health` returned `ok`, anonymous GET returned `401`, authenticated GET/PUT/GET smoke passed, and the tracking record remained empty (`item_count=0`). Tracking is metadata only: `done` does not change readiness score, release governance policy, provider state, workflow installation, or release blocking defaults.
- Enterprise Route Auth Regression Hardening ticket `KAN-61` closes the auth-scope regression hardening pass for Enterprise admin routes. PR `#176` merged on `main` as `6483c53`. It treats all `/enterprise/*` routes as sensitive admin paths for stale-auth-cache fail-closed behavior and adds a DB-backed integration matrix covering anonymous `401`, non-admin `403`, global-admin missing `org_name` `400`, cross-tenant scoped-admin `403`, and valid scoped-admin `200` outcomes across adoption profile, onboarding checklist tracking, release approvals, and release governance evaluation routes. Report: `docs/reports/enterprise-route-auth-regression-hardening-2026-05-02.md`. Local validation passed with backend `cargo fmt`, `cargo check`, `cargo clippy -- -D warnings`, focused auth matrix test, full `cargo test` (`193` tests), Docker-backed focused test against temporary local Postgres on port `55433`, `git diff --check`, and `.\scripts\security\publication_guard.ps1`. Post-merge `CI` run `25245741318` and `Release Readiness Gate` run `25245741327` passed. Render deploy `dep-d7qph6vlk1mc73d5lni0` reached `live`; production smoke returned `/health` `200`, anonymous adoption profile `401`, authenticated adoption profile `200`, and authenticated checklist tracking `200`. No DB migration or provider/customer repository mutation was needed.
- Enterprise Route Auth Smoke Automation ticket `KAN-62` operationalizes the KAN-61 production smoke through `scripts/control-plane/validate_enterprise_route_auth_smoke.ps1` and `.github/workflows/enterprise-route-auth-smoke.yml`. PR `#178` merged on `main` as `e86c6bc`. The workflow runs manually and weekly, validates public `/health`, anonymous Enterprise `401` responses, and authenticated Enterprise `200` responses, then uploads `enterprise-route-auth-smoke-{run_id}`. Design: `docs/design/enterprise-route-auth-smoke-automation-mvp.md`. Report: `docs/reports/enterprise-route-auth-smoke-automation-2026-05-02.md`. Runbook: `docs/runbooks/enterprise-self-service-adoption.md`. Local production validation passed with `9` checks and `0` failures; missing-key mode wrote safe `skipped` evidence; `git diff --check` and `.\scripts\security\publication_guard.ps1` passed. Post-merge `CI` run `25246267909` and `Release Readiness Gate` run `25246267897` passed. First manual workflow dispatch run `25246304135` passed and uploaded artifact `enterprise-route-auth-smoke-25246304135`, ID `6761394808`, not expired. The smoke writes no API key, Authorization header, `.env` value, provider token, or response body, and it does not mutate providers, customer repositories, branch protection, release defaults, or database schema.
- Enterprise Route Auth Smoke Artifact Monitor ticket `KAN-63` adds `.github/workflows/enterprise-route-auth-smoke-artifact-monitor.yml` to validate freshness of artifacts produced by `enterprise-route-auth-smoke.yml`. PR `#180` merged on `main` as `4342947`. Design: `docs/design/enterprise-route-auth-smoke-artifact-monitor-mvp.md`. Report: `docs/reports/enterprise-route-auth-smoke-artifact-monitor-2026-05-02.md`. Runbook: `docs/runbooks/enterprise-self-service-adoption.md`. Local validation using `scripts/control-plane/validate_github_evidence_report_artifact.ps1` passed against workflow `enterprise-route-auth-smoke.yml`, source run `25246304135`, and source artifact `enterprise-route-auth-smoke-25246304135`, ID `6761394808`; `git diff --check` and `.\scripts\security\publication_guard.ps1` passed. Post-merge `CI` run `25246990171` and `Release Readiness Gate` run `25246990161` passed. First manual monitor workflow run `25247025700` passed and uploaded artifact `enterprise-route-auth-smoke-artifact-monitor`, ID `6761616364`, not expired. The monitor reads GitHub artifact metadata only, does not read `.env`, provider secrets, or GitGov API keys, does not rerun the smoke itself, and does not mutate providers, customer repositories, branch protection, or release defaults.
- Enterprise Route Auth Smoke Trend Report ticket `KAN-64` adds `.github/workflows/enterprise-route-auth-smoke-trend-report.yml` and `scripts/control-plane/generate_enterprise_route_auth_smoke_trend_report.ps1` to summarize recent `enterprise-route-auth-smoke-*` artifacts over time. PR `#182` merged on `main` as `45eec39`. Design: `docs/design/enterprise-route-auth-smoke-trend-report-mvp.md`. Report: `docs/reports/enterprise-route-auth-smoke-trend-2026-05-02.md`. Runbook: `docs/runbooks/enterprise-self-service-adoption.md`. Local validation passed against workflow `enterprise-route-auth-smoke.yml`, source run `25246304135`, source artifact `enterprise-route-auth-smoke-25246304135`, ID `6761394808`, with latest status `passed`, `9` passed checks, `0` failed checks, and trend `stable`; `git diff --check` and `.\scripts\security\publication_guard.ps1` passed. Post-merge `CI` run `25247275328` and `Release Readiness Gate` run `25247275146` passed. First manual trend workflow run `25247310737` passed and uploaded artifact `enterprise-route-auth-smoke-trend-report`, ID `6761702022`, not expired. The trend reads GitHub artifact metadata and sanitized auth-smoke JSON only, does not read `.env`, provider secrets, or GitGov API keys, does not probe routes directly, and does not mutate providers, customer repositories, branch protection, or release defaults.
- Enterprise Route Auth Smoke Trend Artifact Monitor ticket `KAN-65` adds `.github/workflows/enterprise-route-auth-smoke-trend-artifact-monitor.yml` to validate freshness of artifacts produced by `enterprise-route-auth-smoke-trend-report.yml`. PR `#184` merged on `main` as `8bd9cf0`. Design: `docs/design/enterprise-route-auth-smoke-trend-artifact-monitor-mvp.md`. Report: `docs/reports/enterprise-route-auth-smoke-trend-artifact-monitor-2026-05-02.md`. Runbook: `docs/runbooks/enterprise-self-service-adoption.md`. Local validation using `scripts/control-plane/validate_github_evidence_report_artifact.ps1` passed against workflow `enterprise-route-auth-smoke-trend-report.yml`, source run `25247310737`, and source artifact `enterprise-route-auth-smoke-trend-report`, ID `6761702022`; `git diff --check` and `.\scripts\security\publication_guard.ps1` passed. Post-merge `CI` run `25247484224` and `Release Readiness Gate` run `25247484227` passed. First manual monitor workflow run `25247519159` passed and uploaded artifact `enterprise-route-auth-smoke-trend-artifact-monitor`, ID `6761758944`, not expired. The monitor reads GitHub artifact metadata only, does not read `.env`, provider secrets, or GitGov API keys, does not rerun the trend itself, and does not mutate providers, customer repositories, branch protection, or release defaults.
- Enterprise Route Auth Smoke Trend Enforcement ticket `KAN-66` adds `.github/workflows/enterprise-route-auth-smoke-trend-enforcement.yml` and enforcement switches to `scripts/control-plane/generate_enterprise_route_auth_smoke_trend_report.ps1`. PR `#186` merged on `main` as `004eeea`. Design: `docs/design/enterprise-route-auth-smoke-trend-enforcement-mvp.md`. Report: `docs/reports/enterprise-route-auth-smoke-trend-enforcement-2026-05-02.md`. Runbook: `docs/runbooks/enterprise-self-service-adoption.md`. Local enforcement validation passed against workflow `enterprise-route-auth-smoke.yml`, source run `25246304135`, source artifact `enterprise-route-auth-smoke-25246304135`, ID `6761394808`, with latest counts `9` passed and `0` failed, failed-check delta `0`, and enforcement status `pass`; `git diff --check` and `.\scripts\security\publication_guard.ps1` passed. Post-merge `CI` run `25247711351` and `Release Readiness Gate` run `25247711349` passed. First manual enforcement workflow run `25247747284` passed and uploaded artifact `enterprise-route-auth-smoke-trend-enforcement`, ID `6761818040`, not expired. Enforcement stays workflow-scoped and opt-in: it fails only when the latest parsed trend exceeds configured failed-check tolerance, failure count increases, or the latest successful source run lacks a parseable artifact.
- Enterprise Route Auth Smoke Trend Enforcement Artifact Monitor ticket `KAN-67` adds `.github/workflows/enterprise-route-auth-smoke-trend-enforcement-artifact-monitor.yml` to validate freshness of artifacts produced by `enterprise-route-auth-smoke-trend-enforcement.yml`. PR `#188` merged on `main` as `78d4878`. Design: `docs/design/enterprise-route-auth-smoke-trend-enforcement-artifact-monitor-mvp.md`. Report: `docs/reports/enterprise-route-auth-smoke-trend-enforcement-artifact-monitor-2026-05-02.md`. Runbook: `docs/runbooks/enterprise-self-service-adoption.md`. Local validation using `scripts/control-plane/validate_github_evidence_report_artifact.ps1` passed against workflow `enterprise-route-auth-smoke-trend-enforcement.yml`, source run `25247747284`, and source artifact `enterprise-route-auth-smoke-trend-enforcement`, ID `6761818040`, not expired; `git diff --check` and `.\scripts\security\publication_guard.ps1` passed. Post-merge `CI` run `25247988131` and `Release Readiness Gate` run `25247988122` passed. First manual monitor workflow run `25248025190` passed and uploaded artifact `enterprise-route-auth-smoke-trend-enforcement-artifact-monitor`, ID `6761892441`, not expired. The monitor reads GitHub artifact metadata only, does not read `.env`, provider secrets, or GitGov API keys, does not rerun the enforcement itself, and does not mutate providers, customer repositories, branch protection, or release defaults.
- Product UX focus ticket `KAN-68` records the post-`KAN-67` operating decision: stop adding endless standalone hardening/features by default and make the next product stage easier to use through an Enterprise Action Center UX. Design: `docs/design/enterprise-action-center-ux-focus.md`. Report: `docs/reports/enterprise-action-center-ux-focus-2026-05-02.md`. Roadmap: `docs/design/enterprise-self-service-and-ai-copilot-roadmap.md`. Pending product work is `KAN-69 - Enterprise Action Center guided UX`, dashboard-first, reusing existing adoption/profile/provider/workflow/readiness/remediation/checklist/release-governance/copilot capabilities. Documentation cleanup started with `KAN-70 - Documentation reality audit and stale docs cleanup`; `KAN-71 - Backend/API/schema documentation reality audit`, `KAN-72 - Desktop/dashboard documentation reality audit`, and `KAN-73 - CI/workflows/release automation documentation reality audit` are merged. CI helper/runtime follow-up `KAN-74` merged through PR `#198` as `ae5771b`: branch-protection helper defaults now match live checks and Secret Scan now uses direct Gitleaks CLI execution with PR/push commit ranges instead of `gitleaks/gitleaks-action@v2`. Public documentation cleanup `KAN-75 - Public web roadmap claims documentation audit` merged through PR `#200` as `b393a82`: public web docs, roadmap/context/product-state docs, and stale public claims were aligned with current Jira, governance, Render, risk-outcome, pricing, metadata, and Next.js facts; it keeps `KAN-69` pending and does not implement Action Center UI. Public agent visibility follow-up `KAN-76` merged through PR `#202` as `8f311f2`: `docs/AGENT_PUBLIC_CONTEXT.md` gives external agents sanitized current context without force-adding restricted forensic/strategy docs and avoids presenting mitigated findings as active backlog. These documentation tickets should update living docs against repository reality and explicitly keep `KAN-69` pending. New work should now be accepted only when it improves usability, packages existing capabilities into a clearer workflow, fixes a real bug, closes a confirmed security/production risk, or supports an explicit customer-selected policy. Do not continue the monitor/trend/enforcement chain by default.

## Verified State

- Render backend health endpoint passed on `https://gitgov-api.onrender.com/health`.
- Local ignored `GITGOV_API_KEY` was validated against production `https://gitgov-api.onrender.com/stats` with HTTP `200`; Render does not need a `GITGOV_API_KEY` env var for current DB-backed admin auth, though setting it there can be used as bootstrap consistency.
- Local Docker validation stack was refreshed on 2026-04-28: Docker Desktop engine became ready, `gitgov-sonarqube`, `gitgov-sonarqube-db`, and `gitgov-jenkins` were running, and the local backend health endpoint responded on `http://127.0.0.1:3001/health`.
- Manual Jira ingest to production was validated with Bearer `GITGOV_API_KEY`, `x-gitgov-jira-secret`, and `org_name=yohandry10`; the previous `401` diagnosis was a missing Jira secret header, not a bad GitGov API key.
- GitGov website publication validation completed through `KAN-12`: PR `#77` merged to `main` as commit `a0a4174`, PR checks passed after rerunning a transient `Workflow Lint` download failure, and post-merge `CI` run `24974947818` plus `Release Readiness Gate` run `24974947816` both passed.
- Deployment documentation drift was cleaned so Render is documented as current production, GitHub/Jira webhooks are documented as already configured, and domain/`certbot` work is marked as optional for self-hosted/custom-domain migrations.
- GitGov Render backend has policy and Sonar-style pipeline evidence for `yohandry10/Git-Gov`; last observed correlation sample contained 12 Sonar/Jenkins evidence items for `main`.
- GitHub-hosted matrix validation passed on run `24877293195`.
- Job `Validate quality_gates warn/block matrix` passed on job `72836755674`.
- Local SonarQube API token validation passed with `SONAR_TOKEN`; project `yohandry10_git-gov` quality gate was `OK`.
- Local Jenkins API validation passed through `/whoAmI/api/json`; authenticated user is `admin`.
- Local Jenkins job API validation passed for `gitgov-demo-pipeline`; last observed build was `#30`, result `SUCCESS`, not building.
- Jira Cloud API validation passed through `/rest/api/3/myself` and `/rest/api/3/project/KAN`.
- Jira project `KAN` is reachable and accepts issue type `Tarea`.
- Jira issues `KAN-4`, `KAN-5`, and `KAN-6` were created for GitGov traceability validation.
- Jira issues `KAN-4`, `KAN-5`, and `KAN-6` were ingested into GitGov through `POST /integrations/jira`.
- Native signed Jira webhook delivery was validated end-to-end by updating `KAN-6` in Jira Cloud and observing `last_ingest_at` advance in GitGov.
- GitGov Jira correlation was validated with `KAN-6`; the main commit from `docs(KAN-6): document Jira API access (#21)` produced one commit-ticket correlation.
- Jira ticket coverage for `yohandry10/Git-Gov` over the 720h validation window was last observed at `1/25` commits with tickets (`4.0%`) after additional GitHub-hosted merge commits were ingested.
- Repo/branch-scoped readiness validation for `yohandry10/Git-Gov` on `main` was refreshed on 2026-04-28: standard readiness `91/100` against target `75`, signal coverage `3/3`, pipeline success `98.67%`, Sonar pass `98.67%`, and Jira coverage `66.22%`.
- Secret-safe provider access validation can be run with `.\scripts\control-plane\validate_provider_access.ps1 -IncludeReleaseReadiness`; latest KAN-16 validation returned all checks `ok`, readiness `92/100`, pipeline success `98.81%`, Jira ticket coverage `69.88%`, and Sonar pass `98.81%`.
- Local SonarQube self-hosted runner activation is documented but not enabled by default. Keep `sonar-governance.yml` on GitHub-hosted/non-blocking behavior until a dedicated runner with label `gitgov-local-sonar` is online and validated.
- Dedicated Jira traceability coverage validation can be run with `.\scripts\control-plane\validate_jira_traceability_coverage.ps1 -RefreshCorrelations -MinCoverage 50`; latest KAN-19 run passed with coverage `96.67%`, `scanned_prs=57`, and `correlations_created=0`.
- Risk Tier Baseline Calibration scheduled run `24999681550` failed on 2026-04-27 because the workflow used PowerShell array splatting and passed `-RepoFullName` positionally into the `Tier` parameter. Desktop Updater Readiness had the same invocation pattern and produced optional job failures. Both workflows were corrected to hashtable splatting and locally validated with the target scripts. Follow-up manual runs `25049577630` and `25049782826` proved calibration succeeded but artifact upload must use the deterministic report path directly instead of relying on a step output. Final manual validation run `25049984199` passed and uploaded artifact `risk-tier-baseline-25049984199` ID `6682824924`.
- GitHub repository webhook ID `610772988` is active and delivered real `pull_request`, `push`, `issue_comment`, `check_run`, `check_suite`, and `status` events to Render with HTTP `200`.
- GitHub PR merge delivery validation with `KAN-4` titles was completed in production:
  - PR merge evidence was materialized in `pull_request_merges`.
  - PR-title correlations were created for merge/head SHAs using `source=pr_title`.
  - Latest validated GitHub redelivery returned HTTP `200` and `processed=true`.
- GitHub webhook ingestion includes `pull_request_review_comment` and PR-linked `issue_comment`; these events are stored as first-class evidence and can create commit-ticket correlations from ticket IDs in comment/title text.
- GitHub webhook extraction contract tests cover `check_run`, `check_suite`, `status`, and `pull_request_review_comment`; run `cargo test github_webhook_tests` from `gitgov/gitgov-server` after changing webhook evidence parsing.
- Post-merge validation for GitHub webhook extraction contract tests passed on `main` commit `946fac3`: CI run `24927816238`, Quality Gate Policy Matrix run `24927816230`, and Release Readiness Gate run `24927816225`.
- Admin dashboard GitHub reporting includes an executive evidence coverage summary in `EventBreakdownGrid`: PR lifecycle, reviews, PR comments, and checks/status are collapsed to `n/4` coverage with missing signal labels.
- Admin dashboard GitHub reporting includes a local trend widget `GitHubEvidenceTrendWidget`; it stores operator-captured evidence snapshots in browser `localStorage` under `gitgov.dashboard.github_evidence_trend` and does not query GitHub Actions or provider tokens.
- Post-merge validation for the executive GitHub evidence dashboard summary passed on `main` commit `01d275c`: CI run `24938441269`, Quality Gate Policy Matrix run `24938441278`, and Release Readiness Gate run `24938441273`.
- Post-merge validation for the dashboard GitHub evidence trend widget passed on `main` commit `74a51a5`: CI run `24940280762`, Quality Gate Policy Matrix run `24940280775`, and Release Readiness Gate run `24940280751`.
- Admin dashboard audit exports package the same GitHub executive evidence summary into downloaded JSON under `executive_summary.github_evidence`; raw export records remain under `data`.
- Post-merge validation for GitHub evidence export packaging passed on `main` commit `458c048`: CI run `24938795096`, Quality Gate Policy Matrix run `24938795085`, and Release Readiness Gate run `24938795100`.
- `scripts/control-plane/generate_github_evidence_report.ps1` generates a standalone Markdown executive report from `/stats.github_events.by_type` or an offline stats JSON fixture. Use `-StatsJsonPath` for token-free validation and `-GitGovUrl`/`-ApiKey` for live Control Plane reporting.
- `.github/workflows/github-evidence-report.yml` runs the GitHub evidence executive report generator manually or weekly on Monday 13:23 UTC, uploads the Markdown artifact, and skips cleanly when `GITGOV_URL` or `GITGOV_API_KEY` is missing.
- Manual GitHub evidence report workflow validation passed on run `24939329055` for `main` commit `3935c21`; artifact `github-evidence-executive-report` was uploaded successfully.
- `scripts/control-plane/validate_github_evidence_report_artifact.ps1` validates operational freshness of the GitHub evidence report artifact by querying GitHub Actions for the latest successful `github-evidence-report.yml` run, confirming artifact `github-evidence-executive-report` exists, is not expired, and is within the configured max age.
- `.github/workflows/github-evidence-artifact-monitor.yml` runs the artifact freshness monitor manually or weekly on Tuesday 14:07 UTC and uploads `github-evidence-artifact-monitor` JSON evidence.
- Local validation of the artifact monitor passed against workflow run `24939329055`; artifact `6642253304` was fresh and not expired.
- First GitHub-hosted validation of the artifact monitor passed on workflow run `24939815276`; artifact `github-evidence-artifact-monitor` ID `6642391452` uploaded successfully and was not expired.
- `scripts/control-plane/generate_github_evidence_trend_report.ps1` generates Markdown/JSON trend history by downloading recent `github-evidence-executive-report` artifacts from successful `github-evidence-report.yml` runs and parsing status, coverage, and missing signal fields.
- `.github/workflows/github-evidence-trend-report.yml` runs the trend report manually or weekly on Tuesday 14:17 UTC and uploads artifact `github-evidence-trend-report`.
- Operational use of the GitHub evidence dashboard, Markdown report, artifact freshness monitor, and trend report is documented in `docs/runbooks/github-evidence-operations.md`.
- Post-merge validation for the GitHub evidence operations runbook passed on `main` commit `7577f90`: CI run `24940874607`, Quality Gate Policy Matrix run `24940874602`, Release Readiness Gate run `24940874616`, Secret Scan run `24940874599`, SonarQube Governance run `24940874600`, Public Naming Guard run `24940874603`, Governance Correlation Smoke run `24940874611`, and Desktop Updater Readiness run `24940874597`.
- GitHub evidence operational adoption baseline completed on 2026-04-25:
  - Executive report workflow run `24941348198` succeeded on `main` commit `65613b0`; artifact `github-evidence-executive-report` ID `6642829154`.
  - Artifact monitor workflow run `24941358185` succeeded; artifact `github-evidence-artifact-monitor` ID `6642831722`.
  - Trend workflow run `24941363195` succeeded; artifact `github-evidence-trend-report` ID `6642833188`.
  - Local artifact freshness monitor returned `PASS`; latest artifact was not expired and age was `0.02h`.
  - Local trend parsed `2` reports and still observed `Sin evidencia` / `0/4 signals`; Jira follow-up `KAN-7` tracks the data-quality/scope investigation.
- `KAN-7` GitHub evidence stats root cause:
  - `github-evidence-report.yml` reads `GET /stats.github_events.by_type`.
  - Existing `supabase_schema_v18.sql` and `supabase_schema_v19.sql` optimized `get_audit_stats` but returned zeroed GitHub stats.
  - Migration `supabase_schema_v22.sql` restores real `github_events` totals, daily counts, `by_type`, and `active_repos` while preserving v19 violation decision semantics.
  - Postcheck file: `gitgov/gitgov-server/supabase/checks/v22_postcheck.sql`.
  - Production DB migration v22 was applied with `psql`; `v22_postcheck.sql` passed.
  - Post-migration `/stats.github_events.by_type` showed real evidence counts, including `pull_request=75`, `issue_comment=93`, `check_run=1937`, `check_suite=599`, and `status=148`.
  - Local live executive report generated `Parcial` / `3/4 signals`; remaining missing signal is `Reviews` because the current sample lacks `pull_request_review` events.
  - GitHub-hosted validation passed: report run `24942000355` artifact `6643010178`, artifact monitor run `24942008460` artifact `6643012934`, trend run `24942016196` artifact `6643015713`.
  - To close the remaining `Reviews` signal, create/use a Jira-traceable PR and have a reviewer submit a GitHub PR review event; review comments alone count under `PR comments`, not `Reviews`.
  - PR `#71` created a real `pull_request_review` event; `/stats.github_events.by_type.pull_request_review` reached `1`.
  - Local live report after PR `#71` review validation generated `Completo` / `4/4 signals` with evidence file `docs/reports/github-evidence-executive-report-prod-review-v22-2026-04-25.md`.
  - GitHub-hosted post-review validation passed after PR `#71` merged on `main` commit `0a7a230`: report run `24942351831` generated `Completo` / `4/4 signals`, monitor run `24942357291` returned `PASS`, and trend run `24942362269` reported latest coverage `4/4 signals`.
  - Latest GitHub evidence report artifact ID after review validation: `6643110541`; it is the current cloud evidence reference for complete PR lifecycle, reviews, PR comments, and checks/status coverage.
- Admin dashboard Risk Outcomes now includes informational `Time-to-Evidence` and `MTTR pipeline` metrics from Jenkins commit-pipeline correlations:
  - `Time-to-Evidence` is commit timestamp to correlated pipeline ingestion timestamp.
  - `MTTR pipeline` is recoverable non-green Jenkins pipeline event to next successful run for the same job.
  - Duplicate pipeline evidence is ignored before calculating samples.
  - These metrics render `N/A` with insufficient evidence and are not part of composite risk/readiness scoring until tier-specific SLOs are calibrated.
  - Local validation passed with `npm test -- --run src/test/components/dashboard-helpers.test.ts`, full `npm test -- --run`, `npm run typecheck`, `npm run lint`, `git diff --check`, and `.\scripts\security\publication_guard.ps1`.
  - Post-merge GitHub-hosted validation passed on `main` commit `adb5399`: CI `24941724773`, Quality Gate Policy Matrix `24941724754`, Release Readiness Gate `24941724756`, Secret Scan `24941724779`, SonarQube Governance `24941724778`, Public Naming Guard `24941724766`, Governance Correlation Smoke `24941724751`, and Desktop Updater Readiness `24941724750`.
- Local live validation of the trend generator parsed workflow run `24939329055` and produced a 1-report trend with latest coverage `0/4 signals`; this reflects the existing `/stats.github_events.by_type` visibility note, not a secret/config leak.
- First GitHub-hosted validation of the trend workflow passed on run `24940027811` for `main` commit `a58ae81`; artifact `github-evidence-trend-report` ID `6642453325` uploaded successfully and was not expired.
- Post-merge validation for the trend workflow rollout passed on `main` commit `a58ae81`: CI run `24940024455`, Quality Gate Policy Matrix run `24940024458`, and Release Readiness Gate run `24940024457`.
- GitHub merged PR title ingestion creates commit-ticket correlations for the merge commit SHA when the PR title contains a ticket ID, so future `main` merge commits can count toward Jira ticket coverage.
- `POST /integrations/jira/correlate` also scans recent merged PR titles as a backfill path for historical ticket coverage.
- Last production PR-title backfill validation for `KAN-4` observed:
  - `scanned_prs=2`
  - `correlations_created=2`
  - four `KAN-4` correlation rows across validated merge/head SHAs
- Coverage/readiness query semantics were updated after the `33.33%` observation: ticket coverage now includes materialized PR merge commits in addition to desktop/client commit events. After deploying this change, re-run `/integrations/jira/correlate` and `/integrations/jira/ticket-coverage` for `yohandry10/Git-Gov` on `main` to confirm production readiness movement.
- Production validation after deploy `0494648` completed:
  - Render deploy for `fix(KAN-4): count PR merges in ticket coverage (#35)` reached `live`.
  - `/health` returned `ok`.
  - Jira correlation backfill scanned `4` PRs and created `0` new rows because existing correlations were already present.
  - `/integrations/jira/ticket-coverage?repo_full_name=yohandry10%2FGit-Gov&branch=main&hours=720` returned `total_commits=30`, `commits_with_ticket=5`, `coverage_percentage=16.67`.
  - `validate_release_readiness_gate.ps1` passed for `yohandry10/Git-Gov` on `main`: readiness `77/100` vs target `75`, signal coverage `3/3`, pipeline success `96.77%`, Sonar pass `96.77%`, Jira coverage `16.67%`.
- Production validation after traceability guard rollout completed:
  - Jira correlation backfill scanned `8` PRs and created `0` new rows because relevant rows already existed.
  - Ticket coverage for `yohandry10/Git-Gov`, branch `main`, 720h returned `total_commits=34`, `commits_with_ticket=9`, `coverage_percentage=26.47`.
  - Release readiness passed with readiness `79/100` vs target `75`, signal coverage `3/3`, pipeline success `97.14%`, Sonar pass `97.14%`, Jira coverage `26.47%`.
  - This confirms the branch/PR/commit Jira-ID guardrail is improving coverage through the PR-title merge evidence path.
- Scheduled release readiness monitoring is configured in `.github/workflows/release-readiness-gate.yml`:
  - Runs daily at `10:17 UTC`.
  - Refreshes Jira correlations through `POST /integrations/jira/correlate` before scoring.
  - Uploads both `release-readiness-gate-<run_id>.json` and `jira-correlation-refresh-<run_id>.json`.
  - Fails scheduled runs when readiness is below the configured standard target.
- First GitHub-hosted validation after scheduling passed on run `24927045053`:
  - Event: `push` on `main` for commit `a94114c`.
  - Jira correlation refresh artifact was generated.
  - Readiness was `81/100` against target `75`.
  - Signal coverage was `3/3`.
- GitHub Actions Node 24 compatibility upgrade is documented at `docs/reports/github-actions-node24-upgrade-2026-04-25.md`.
- First GitHub-hosted validation after the full Node 24 action-runtime upgrade passed:
  - `main` commit `3f4c601`.
  - CI run `24927274092` passed with `actions/checkout@v6`, `actions/setup-node@v6`, and `pnpm/action-setup@v5`.
  - The previous Node.js 20 action-runtime annotation was not present in the CI run output.
  - Release Readiness Gate run `24927274091` passed with readiness `82/100`, target `75`, and signal coverage `3/3`.
- Production tier/SLO calibration completed on 2026-04-25 after refreshing Jira PR correlations:
  - Jira backfill scanned `14` merged PRs and created `0` new correlations.
  - Tier baseline reports were generated under `docs/reports/risk-tier-baseline-prod-2026-04-25/`.
  - Critical: readiness `96/100`, composite risk `4/100`, traceability gap `11.8%`, no SLA breaches.
  - Standard: readiness `95/100`, composite risk `4/100`, traceability gap `11.8%`, no SLA breaches.
  - Internal: readiness `96/100`, composite risk `4/100`, traceability gap `11.8%`, no SLA breaches.
  - Domain SLO validation reports were generated under `docs/reports/domain-slo-validation-prod-2026-04-25/`.
  - Domains `core-platform`, `standard-services`, and `internal-tools` all passed after scoping targets to `org_name=yohandry10`.
- Domain SLO target config validation is enforced by `scripts/control-plane/validate_domain_slo_target_config.ps1` in CI and `.github/workflows/domain-slo-validation.yml`, requiring explicit `org_name`, `repo_full_name`, and `branch` scope.
- Post-merge validation for SLO target config guardrail passed on `main` commit `f0a3470`: CI run `24927603357`, Quality Gate Policy Matrix run `24927603365`, and Release Readiness Gate run `24927603352`.

## Safety Rules

- Never commit `.env`, `.env.local`, `.env.*.local`, `.mcp.json`, or files under `secrets/`.
- Never print API keys, Render tokens, GitHub tokens, Jenkins tokens, or Sonar tokens.
- Never paste provider tokens into GitHub Actions variables; use GitHub Actions secrets for sensitive values.
- Do not revert unrelated dirty files in the user's main worktree.
- Prefer `gh` for GitHub operations instead of browser steps.
- Prefer Render API for Render checks when `RENDER_API_KEY` is present.
- Prefer Jenkins API for Jenkins checks when `JENKINS_API_TOKEN` is present.
- Prefer SonarQube API for Sonar checks when `SONAR_TOKEN` is present.
- Prefer Jira API for Jira checks when `JIRA_API_TOKEN` is present.
- After any major access/configuration/deployment/validation change, update `AGENTS.md` and the relevant document under `docs/` before finalizing the PR. This repository relies on docs as persistent agent memory.
- Future branches, commits, and PR titles should include Jira ticket IDs to preserve GitGov ticket coverage and release readiness.
