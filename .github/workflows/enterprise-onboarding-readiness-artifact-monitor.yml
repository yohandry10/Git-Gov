name: Enterprise Onboarding Readiness Artifact Monitor

on:
  workflow_dispatch:
    inputs:
      max_age_hours:
        description: "Maximum accepted age for the latest enterprise onboarding readiness artifact"
        required: false
        default: "192"
        type: string
  schedule:
    - cron: "07 14 * * 4"

permissions:
  actions: read
  contents: read

env:
  GITGOV_ENV: ci

jobs:
  validate-artifact:
    name: Validate enterprise onboarding readiness artifact
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Assert explicit runtime env
        run: bash "$GITHUB_WORKSPACE/.github/scripts/assert-gitgov-env.sh"

      - name: Validate latest onboarding readiness artifact
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
          INPUT_MAX_AGE_HOURS: ${{ inputs.max_age_hours }}
          GITHUB_REPOSITORY_NAME: ${{ github.repository }}
        run: |
          $maxAgeHours = $env:INPUT_MAX_AGE_HOURS
          if ([string]::IsNullOrWhiteSpace($maxAgeHours)) {
            $maxAgeHours = "192"
          }

          .\scripts\control-plane\validate_github_evidence_report_artifact.ps1 `
            -Repository $env:GITHUB_REPOSITORY_NAME `
            -WorkflowFile "enterprise-onboarding-readiness.yml" `
            -ArtifactNamePrefix "enterprise-onboarding-readiness-" `
            -MaxAgeHours ([int]$maxAgeHours) `
            -OutputPath "out/enterprise-onboarding-readiness-artifact-monitor.json"

      - name: Upload monitor summary
        uses: actions/upload-artifact@v7
        with:
          name: enterprise-onboarding-readiness-artifact-monitor
          path: out/enterprise-onboarding-readiness-artifact-monitor.json
          if-no-files-found: error
